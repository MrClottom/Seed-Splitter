;(function init() {
  loadWordList('bip39-en', '#recombination .wordlist > textarea')
  loadWordList('bip39-en', '#secretSplit .wordlist > textarea ')
})()

const register = (selector, as, func, sendTo) => {
  const el = document.querySelector(selector)
}
